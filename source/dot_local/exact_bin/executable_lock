#!/bin/sh
set -euC
# revert() {
#   xset dpms 0 0 0
# }
# trap revert HUP INT TERM
# every 5 seconds
# xset +dpms dpms 5 5 5
# force off the first time
# sleep .5 && xset dpms force off &
i3lock -n -e -c 000000
# revert
