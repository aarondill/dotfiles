Section "Device"
   Identifier  "Intel Graphics"
   Driver      "intel"
# The modesetting driver doesn't *yet* support TearFree on Ubuntu
# When it does, it should be used instead of `intel`
# Driver      "modesetting"
   Option      "TearFree"  "true"
EndSection
